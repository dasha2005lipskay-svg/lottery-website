<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Форма сбора данных и лотерея</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@6.4.0/css/all.min.css">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #f5f5f5 0%, #e5e5e5 100%);
        }
        
        .modal-overlay {
            background-color: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(5px);
        }
        
        .lottery-container {
            background: linear-gradient(135deg, #ffffff 0%, #f5f5f5 100%);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        }
        
        .word-display {
            background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
            color: white;
            font-weight: bold;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }
        
        .spin-animation {
            animation: spin 0.5s ease-in-out;
        }
        
        @keyframes spin {
            0% { transform: rotateY(0deg) scale(1); }
            50% { transform: rotateY(180deg) scale(1.1); }
            100% { transform: rotateY(360deg) scale(1); }
        }
        
        .form-input:focus {
            border-color: #dc2626;
            box-shadow: 0 0 0 3px rgba(220, 38, 38, 0.1);
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
            transition: all 0.3s ease;
        }
        
        .btn-primary:hover {
            background: linear-gradient(135deg, #b91c1c 0%, #991b1b 100%);
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(220, 38, 38, 0.3);
        }
        
        .checkbox-custom {
            appearance: none;
            width: 20px;
            height: 20px;
            border: 2px solid #6b7280;
            border-radius: 4px;
            background-color: white;
            cursor: pointer;
            position: relative;
        }
        
        .checkbox-custom:checked {
            background-color: #dc2626;
            border-color: #dc2626;
        }
        
        .checkbox-custom:checked::after {
            content: '✓';
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            color: white;
            font-weight: bold;
            font-size: 14px;
        }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center">
    
    <!-- Модальное окно с формой -->
    <div id="formModal" class="fixed inset-0 modal-overlay flex items-center justify-center z-50">
        <div class="bg-white rounded-lg shadow-2xl p-8 max-w-md w-full mx-4 transform transition-all">
            <div class="text-center mb-6">
                <h2 class="text-2xl font-bold text-gray-800 mb-2">Регистрация участника</h2>
                <p class="text-gray-600">Заполните форму для участия в лотерее</p>
            </div>
            
            <form id="registrationForm" class="space-y-4">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">ФИО *</label>
                    <input type="text" id="fullName" required 
                           class="form-input w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none transition-all"
                           placeholder="Введите ваше полное имя">
                </div>
                
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Email *</label>
                    <input type="email" id="email" required 
                           class="form-input w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none transition-all"
                           placeholder="example@email.com">
                </div>
                
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Номер телефона *</label>
                    <input type="tel" id="phone" required 
                           class="form-input w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none transition-all"
                           placeholder="+7 (999) 123-45-67">
                </div>
                
                <div class="flex items-start space-x-3 mt-6">
                    <input type="checkbox" id="dataProcessing" required class="checkbox-custom mt-1">
                    <label for="dataProcessing" class="text-sm text-gray-700 cursor-pointer">
                        Я согласен на обработку персональных данных и участие в лотерее *
                    </label>
                </div>
                
                <button type="submit" class="btn-primary w-full py-3 px-6 text-white rounded-lg font-semibold">
                    <i class="fas fa-paper-plane mr-2"></i>
                    Зарегистрироваться
                </button>
            </form>
        </div>
    </div>
    
    <!-- Страница лотереи -->
    <div id="lotteryPage" class="hidden w-full max-w-2xl mx-4">
        <div class="lottery-container rounded-2xl p-8 text-center">
            <h1 class="text-4xl font-bold text-gray-800 mb-2">🎲 Лотерея слов</h1>
            <p class="text-gray-600 mb-8">Нажмите кнопку и узнайте ваше выигрышное слово!</p>
            
            <div class="mb-8">
                <div id="wordDisplay" class="word-display text-6xl font-bold py-12 px-8 rounded-xl mb-6 min-h-32 flex items-center justify-center">
                    ?
                </div>
                
                <button id="lotteryBtn" class="btn-primary py-4 px-8 text-xl font-semibold text-white rounded-xl">
                    <i class="fas fa-dice mr-2"></i>
                    Участвовать в лотерее
                </button>
            </div>
            
            <div class="bg-gray-100 rounded-lg p-4">
                <h3 class="text-lg font-semibold text-gray-700 mb-2">Возможные призы:</h3>
                <div class="flex flex-wrap justify-center gap-2">
                    <span class="bg-white px-3 py-1 rounded-full text-sm text-gray-600">🍵 кружка</span>
                    <span class="bg-white px-3 py-1 rounded-full text-sm text-gray-600">🍽️ тарелка</span>
                    <span class="bg-white px-3 py-1 rounded-full text-sm text-gray-600">🥤 стакан</span>
                    <span class="bg-white px-3 py-1 rounded-full text-sm text-gray-600">🥓 сало</span>
                    <span class="bg-white px-3 py-1 rounded-full text-sm text-gray-600">🥩 мясо</span>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Google Apps Script URL для отправки данных
        // ВАЖНО: Замените на ваш реальный URL после настройки Google Apps Script
        const GOOGLE_SCRIPT_URL = 'https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec';
        
        // Список слов для лотереи
        const words = ['кружка', 'тарелка', 'стакан', 'сало', 'мясо'];
        
        // Эмодзи для слов
        const wordEmojis = {
            'кружка': '🍵',
            'тарелка': '🍽️',
            'стакан': '🥤',
            'сало': '🥓',
            'мясо': '🥩'
        };
        
        // Элементы DOM
        const formModal = document.getElementById('formModal');
        const lotteryPage = document.getElementById('lotteryPage');
        const registrationForm = document.getElementById('registrationForm');
        const wordDisplay = document.getElementById('wordDisplay');
        const lotteryBtn = document.getElementById('lotteryBtn');
        
        // Показать модальное окно при загрузке страницы
        window.addEventListener('load', () => {
            formModal.classList.remove('hidden');
        });
        
        // Обработка отправки формы
        registrationForm.addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const formData = {
                fullName: document.getElementById('fullName').value,
                email: document.getElementById('email').value,
                phone: document.getElementById('phone').value,
                timestamp: new Date().toLocaleString('ru-RU')
            };
            
            // Показать загрузку
            const submitBtn = registrationForm.querySelector('button[type="submit"]');
            const originalText = submitBtn.innerHTML;
            submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Отправка...';
            submitBtn.disabled = true;
            
            try {
                // Отправка данных в Google Таблицы
                await sendToGoogleSheets(formData);
                
                // Скрыть форму и показать лотерею
                formModal.classList.add('hidden');
                lotteryPage.classList.remove('hidden');
                
                // Показать уведомление об успешной регистрации
                showNotification('Регистрация успешна! Теперь участвуйте в лотерее!', 'success');
                
            } catch (error) {
                console.error('Ошибка отправки данных:', error);
                showNotification('Ошибка регистрации. Попробуйте еще раз.', 'error');
                
                // Восстановить кнопку
                submitBtn.innerHTML = originalText;
                submitBtn.disabled = false;
            }
        });
        
        // Функция отправки данных в Google Таблицы
        async function sendToGoogleSheets(data) {
            // Если у вас еще нет настроенного Google Apps Script, 
            // эта функция будет имитировать отправку
            
            return new Promise((resolve) => {
                setTimeout(() => {
                    console.log('Данные для отправки в Google Таблицы:', data);
                    
                    // Здесь должен быть реальный запрос к Google Apps Script:
                    /*
                    fetch(GOOGLE_SCRIPT_URL, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json',
                        },
                        body: JSON.stringify(data)
                    })
                    .then(response => response.json())
                    .then(result => resolve(result))
                    .catch(error => reject(error));
                    */
                    
                    resolve({ success: true });
                }, 1000);
            });
        }
        
        // Обработка лотереи
        lotteryBtn.addEventListener('click', () => {
            // Анимация вращения
            wordDisplay.classList.add('spin-animation');
            lotteryBtn.disabled = true;
            lotteryBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Крутим...';
            
            setTimeout(() => {
                // Выбор случайного слова
                const randomWord = words[Math.floor(Math.random() * words.length)];
                const emoji = wordEmojis[randomWord];
                
                // Показать результат
                wordDisplay.innerHTML = `${emoji}<br>${randomWord}`;
                wordDisplay.classList.remove('spin-animation');
                
                // Восстановить кнопку
                lotteryBtn.disabled = false;
                lotteryBtn.innerHTML = '<i class="fas fa-dice mr-2"></i>Попробовать еще раз';
                
                // Показать поздравление
                showNotification(`Поздравляем! Ваш приз: ${randomWord} ${emoji}`, 'success');
                
            }, 1000);
        });
        
        // Функция показа уведомлений
        function showNotification(message, type = 'info') {
            const notification = document.createElement('div');
            notification.className = `fixed top-4 right-4 p-4 rounded-lg text-white font-semibold z-50 transform transition-all duration-300 ${
                type === 'success' ? 'bg-green-500' : type === 'error' ? 'bg-red-500' : 'bg-blue-500'
            }`;
            notification.innerHTML = `
                <div class="flex items-center">
                    <i class="fas ${type === 'success' ? 'fa-check-circle' : type === 'error' ? 'fa-exclamation-circle' : 'fa-info-circle'} mr-2"></i>
                    ${message}
                </div>
            `;
            
            document.body.appendChild(notification);
            
            // Показать уведомление
            setTimeout(() => notification.classList.add('translate-x-0'), 100);
            
            // Скрыть через 3 секунды
            setTimeout(() => {
                notification.classList.add('translate-x-full');
                setTimeout(() => document.body.removeChild(notification), 300);
            }, 3000);
        }
        
        // Валидация телефона в реальном времени
        document.getElementById('phone').addEventListener('input', function(e) {
            let value = e.target.value.replace(/\D/g, '');
            if (value.length > 0) {
                if (value[0] !== '7' && value[0] !== '8') {
                    value = '7' + value;
                }
                if (value[0] === '8') {
                    value = '7' + value.slice(1);
                }
            }
            
            let formatted = value;
            if (value.length > 1) {
                formatted = '+7 (' + value.slice(1, 4);
            }
            if (value.length >= 5) {
                formatted += ') ' + value.slice(4, 7);
            }
            if (value.length >= 8) {
                formatted += '-' + value.slice(7, 9);
            }
            if (value.length >= 10) {
                formatted += '-' + value.slice(9, 11);
            }
            
            e.target.value = formatted;
        });
    </script>
</body>
</html>
